apiVersion: apps/v1
kind: Deployment
metadata:
  name: streaming-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: streaming-app
  template:
    metadata:
      labels:
        app: streaming-app
    spec:
      containers:
      - name: streaming-app
        image: vxztu2/24kng_webpages:932be89bcff71c72bdb48e49b4af26942dcf217f
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: django-db-secrets
